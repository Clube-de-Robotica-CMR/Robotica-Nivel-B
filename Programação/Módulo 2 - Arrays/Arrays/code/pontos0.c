//média de três pontos (hardcoded) usando três variáveis

#include <stdio.h>

int main(void)
{
    int ponto1 = 79;
    int ponto2 = 73;
    int ponto3 = 33;

    printf("Media: %i\n", (ponto1 + ponto2 + ponto3) / 3.0);
}